<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.PopUpTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="FloatingButton" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:background">@drawable/tabbackground</item>
        <item name="android:textColor">@color/dirty_white</item>

    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/gray</item>
        <item name="android:shape"> rectangle </item>
        <item name="android:radius">15dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/vivid_cyan</item>
        <item name="android:textColor">@color/darkblue</item>
        <item name="android:textColorPrimary">@color/dirty_white</item>
        <item name="android:colorAccent">@color/brownish_yellow</item>
    </style>

</resources>
